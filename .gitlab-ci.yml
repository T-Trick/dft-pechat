image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html index.css index.js public/
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
