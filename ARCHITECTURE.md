# ARCHITECTURE
Карта связей проекта для ИИ-агентов: читайте этот файл вместо обхода кода. README отсутствует; по составу это автономная копия лендинга «DFT печать».

## Назначение
Одностраничный лендинг «DFT печать» (витрина): услуги, примеры работ, «как заказать», контакты. Чистый статический HTML/CSS/JS без сборки и без бэкенда. Личный кабинет, админ-панель, форма заказа, чат, авторизация и Supabase **удалены** — единственный способ связи с создателями сайта и приёма заказов: кнопки на Telegram и Avito. Запуск — открыть `index.html` в браузере.

## Контакты-CTR (основной контакт с клиентами)
| Канал | Ссылка | Где используется |
|---|---|---|
| Telegram | `https://t.me/glushenkov93` (`@glushenkov93`) | кнопка в навигации (`.menu .cta`), hero, блок после портфолио, секция `#contact`, финальный CTA, футер |
| Avito | `https://avito.ru/user/7eec4768a11737f848122437edc7664c/profile?src=sharing` (профиль с примерами и отзывами) | hero, блок после портфолио, секция `#contact`, финальный CTA, футер |

Ссылки продублированы в `index.html` в нескольких местах — при смене контакта правьте **все** вхождения (поиск по `t.me/` и `avito.ru`).

## Точки входа
| Вход | Что это | Куда ведёт |
|---|---|---|
| `index.html` | страница сайта | `<link href="index.css">`, `<script src="index.js">` |
| `index.js` | вся логика сайта | только UI-логика, внешних запросов нет |

## Граф связей
```
index.html ──> index.css   (стили; классы кнопок контактов: .btn-tg, .btn-avito, .contacts-2, .contact-link)
    └─> index.js
          ├─ мобильное меню (#burger/#menu), липкая навигация (#nav.scrolled)
          ├─ scroll-reveal (.reveal/.visible) и активный якорь меню (IntersectionObserver)
          └─ canvas-частицы (#particles)
```
Внешних зависимостей (CDN, API, хранилища) нет. localStorage/sessionStorage не используются.

## Что менять вместе с чем (контракты)
| Изменили | Обновите обязательно |
|---|---|
| ссылки Telegram/Avito | все вхождения в `index.html` + таблицу выше |
| классы `.btn-tg`/`.btn-avito`/`.contacts-2`/`.contact-link` | `index.css` и все кнопки/карточки в `index.html` |
| id секций (`#services`, `#portfolio`, `#how`, `#contact`) | якоря в навигации и футере, карту `map` в `index.js` |

## Тесты
Тестов нет.

## Ограничения, важные при правках
- Проект — статическая витрина без бэкенда: любая новая функциональность контактов — только внешние ссылки (Telegram/Avito) либо новый бэкенд (осознанное расширение).
- Порядок тегов: `index.js` подключается перед `</body>` и обращается к DOM напрямую — скрипт должен оставаться после разметки.
